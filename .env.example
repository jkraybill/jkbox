# GitHub MCP Server
# Get token from: https://github.com/settings/tokens
# Required scopes: repo, read:org
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Gordo Memory MCP Server - Embedding Provider
# Option 1 (Recommended): Use Ollama (free, local, private)
#   Install: curl https://ollama.ai/install.sh | sh
#   Pull model: ollama pull mxbai-embed-large:latest
#   No environment variable needed - gordo-memory auto-detects Ollama
#
# Option 2 (Fallback): Use OpenAI (paid, cloud)
#   Uncomment and set your OpenAI API key below:
# OPENAI_API_KEY=sk-your_openai_api_key_here

# Server Configuration (for when you build the party game server)
PORT=3000
NODE_ENV=development
